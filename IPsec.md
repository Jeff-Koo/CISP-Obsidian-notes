
### 1. 核心定位与概念

- **协议层级：** 运行于 **网络层（Layer 3）**，对上层应用（TCP/UDP/HTTP 等）透明。
- **核心目的：** 在不安全的网络（如互联网）上建立安全的通信管道（VPN 隧道），提供**机密性（Confidentiality）、完整性（Integrity）、身份鉴别（Authentication）与防重放攻击（Anti-Replay）**。
- **本质架构：** **IPsec 不是单一协议，而是一个协议族（Framework）**，由安全协议（AH/ESP）、密钥协商协议（IKE）及密码算法组合而成。


### 2. IPsec 两大核心安全协议（AH vs ESP）

| **维度**        | **AH (Authentication Header)**  | **ESP (Encapsulating Security Payload)** |
| ------------- | ------------------------------- | ---------------------------------------- |
| **IP 协议号**    | **51**                          | **50**                                   |
| **机密性（数据加密）** | **不支持**（明文传输）                   | **支持**（对数据载荷加密）                          |
| **完整性与身份认证**  | 支持                              | 支持                                       |
| **防重放攻击**     | 支持                              | 支持                                       |
| **校验保护范围**    | 包含外层 IP 头在内的**整个报文**            | 仅包含 ESP Header 之后的数据及 ESP Trailer        |
| **NAT 穿透兼容性** | **不兼容**（NAT 修改 IP 头会导致 AH 校验失败） | **兼容**（可通过 NAT-T 机制穿透）                   |
| **应用现状**      | 因不支持加密且与 NAT 冲突，**极少单独使用**      | **IPsec VPN 的主流核心选择**                    |

### 3. IPsec 的两种工作模式

```
[ 传输模式 (Transport Mode) ] 
┌───────────┬────────────┬────────────────────────┐
│  原 IP 头  │ IPsec 头   │  TCP / UDP + 数据载荷   │
└───────────┴────────────┴────────────────────────┘
                         |<----- 保护/加密范围 ---->|

[ 隧道模式 (Tunnel Mode) ] 
┌───────────┬────────────┬───────────┬────────────┬────────────────────────┐
│  新 IP 头  │ IPsec 头   │  原 IP 头 │  TCP / UDP  │       数据载荷          │
└───────────┴────────────┴───────────┴────────────┴────────────────────────┘
                         |<----------------- 保护/加密范围 ----------------->|
```

- **传输模式（Transport Mode）：**
    - 不改变原 IP 报文头，仅在原 IP 头与上层载荷之间插入 IPsec 头（AH/ESP）。
    - **应用场景：** 端到端（Host-to-Host）的主机间直接安全通信。

- **隧道模式（Tunnel Mode）：**
    - 会生成全新的**外层 IP 报文头**，将包含原 IP 头在内的整个原始数据包完全隐藏/加密封装在内。
    - **应用场景：** 网关到网关（Site-to-Site VPN）或客户端到网关（Remote Access VPN），是企业 VPN 最常用的模式。


### 4. 密钥管理与建立：IKE 协议（UDP 500 / 4500）

**IKE（Internet Key Exchange）** 负责动态协商加密算法、身份认证并生成对称密钥，分为两个阶段：

- **阶段 1（Phase 1）：建立 IKE SA（安全通道）**
    - **目的：** 在通信双方之间建立一条安全的管理通道，用于保护阶段 2 的协商过程。
    - **模式选择：**
        - **主模式（Main Mode）：** 交换 6 个报文，安全性高，支持身份隐蔽。
        - **野人模式 / 积极模式（Aggressive Mode）：** 交换 3 个报文，协商速度快，适合动态 IP 或 IPsec 远程接入场景（不隐蔽身份）。
            
              
            
- **阶段 2（Phase 2）：建立 IPsec SA（数据通道）**
    - **目的：** 协商实际保护业务数据的安全参数（如 ESP/AH 算法、生命周期、PFS 完美向前安全性）。
    - **模式：** 快速模式（Quick Mode，交换 3 个报文）。


### 5. 核心考点与网络规划优化

1. **安全关联（SA, Security Association）：**
    - SA 是单向的逻辑连接。一对通信双向至少需要建立 **2 个 IPsec SA**。
        
          
        
    - 由 **三元组** 唯一标识：`三元组 = SPI（安全参数索引） + 目的 IP 地址 + 安全协议号（50/51）`。
        
          
        
2. **地址聚合优化：**
    
      
    - 每匹配一条“感兴趣流（ACL）”就会协商建立一对 SA。
        
          
        
    - 在分支机构规划 IP 地址时，使用**可聚合的 IP 地址段**，可以把多条细分子网流量合并匹配同一条策略，显著**减少 SA 的数量与系统资源消耗**。